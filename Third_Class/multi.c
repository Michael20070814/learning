#include <stdio.h>

typedef struct unknown
{
    int coeffiecent;
    int exponent;
    struct unknown *next;
} unknown;

unknown* input(void);
unknown* multi(void);
void print(void);

int main(void)
{

}

unknown* input(void)
{
    unknown *list, *p;

    
}