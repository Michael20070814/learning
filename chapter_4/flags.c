#include <stdio.h>
int main(void)
{
    printf("%x %X %#x \n", 31, 31, 31);/* x表示十六进制  */
    printf("**%d**% d**% d**\n", 42, 42, -42);/*  空格指代正号时会显示前导空格（一个长度），负号则显示负号  */
    printf("**%5d**%5.3d**%05d**%05.3d**\n", 6, 6, 6, 6);//0标记与精度同时出现，0标记则被忽略

    return 0;
}